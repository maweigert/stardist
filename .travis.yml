# As a general rule, the latest release of Keras is compatible with the latest
# release of TensorFlow as well as the previous release of TensorFlow (in most
# cases, it is actually compatible with several prior TF releases, but that is
# not guaranteed).
#
# -- https://github.com/keras-team/keras/issues/10440#issuecomment-397428200

language: python

branches:
  only:
    - master
    - dev

env:
<<<<<<< HEAD
  - TENSORFLOW='tensorflow' KERAS='keras'
=======
  - TENSORFLOW='tensorflow<2' KERAS='keras'
>>>>>>> d58ac9a660a901f0de858dd4cb6c935491a9f6d2

matrix:
  include:
    - os: linux
      python: 3.7
      dist: xenial
    - os: linux
      python: 3.5
    - os: linux
      python: 3.6
    - os: linux
<<<<<<< HEAD
      python: 3.7
      dist: xenial
      env: TENSORFLOW='tensorflow' KERAS='keras' ONLY_INSTALL=1

install:
  - pip install $TENSORFLOW $KERAS
  - if [ -z $ONLY_INSTALL ]; then
       pip install stardist;
    else
       pip install .;
    fi;

script:
  - if [ -z $ONLY_INSTALL ]; then
       echo "only installing";
    else
       pytest -v -s --durations=50 -m "not gpu";
    fi;
=======
      python: 3.6
      dist: xenial
      env: TENSORFLOW='tensorflow<2' KERAS='keras' INSTALL_FROM_SOURCE=1
    - os: linux
      python: 3.7
      dist: xenial
      env: TENSORFLOW='tensorflow<2' KERAS='keras' INSTALL_FROM_SOURCE=1

install:
  - pip install $TENSORFLOW $KERAS
  - if [ -z $INSTALL_FROM_SOURCE ]; then
       pip install .;
    else
       pip install stardist;
    fi;

script:
  - pytest -v --durations=50 -m "not gpu";
>>>>>>> d58ac9a660a901f0de858dd4cb6c935491a9f6d2
